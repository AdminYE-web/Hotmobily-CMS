# แผนปรับปรุงระบบจาก Pure PHP เป็น Laravel และ API

## 1. ภาพรวมโปรเจกต์

โปรเจกต์นี้มีเป้าหมายเพื่อปรับปรุงเว็บไซต์เดิมที่พัฒนาด้วย Pure PHP ให้เป็นระบบใหม่บน Laravel โดยแยกส่วนการแสดงผลออกจาก Business Logic และออกแบบ Backend ให้บริการข้อมูลผ่าน API เป็นหลัก เพื่อให้เว็บไซต์และแอปพลิเคชันมือถือในอนาคตสามารถใช้ Backend ชุดเดียวกันได้

สถานะเริ่มต้นของโปรเจกต์ ณ วันที่จัดทำเอกสาร:

- Backend: Laravel 13
- PHP: 8.3 ขึ้นไป
- Frontend tooling: Vite และ Tailwind CSS 4
- ระบบทดสอบ: PHPUnit
- `original-web/`: โฟลเดอร์สำหรับเก็บ Source Code ของเว็บไซต์ Pure PHP เดิม โดยระบุ `index.php` เป็นหน้า Home เดิมแล้ว
- Laravel application: ขึ้นโครงหน้า Home ใหม่ด้วย Blade และแยก Layout, Header และ Footer แล้ว ส่วนข้อมูลสินค้าและข่าวแบบ Dynamic ยังรอย้าย

## 2. เป้าหมายหลัก

1. ย้ายฟังก์ชันจาก Pure PHP มาเป็น Laravel อย่างเป็นระบบ
2. รักษาพฤติกรรมสำคัญและข้อมูลเดิมระหว่างการย้ายระบบ
3. แยก Business Logic ออกจากหน้าเว็บและ Controller
4. ใช้ API เป็นช่องทางหลักในการเข้าถึงข้อมูลและดำเนินการต่าง ๆ
5. รองรับทั้งเว็บไซต์และแอปมือถือด้วย API ชุดเดียวกัน
6. เพิ่มความปลอดภัย ความสามารถในการทดสอบ และความสะดวกในการบำรุงรักษา
7. สามารถทยอยเปิดใช้งานระบบใหม่เป็นโมดูล เพื่อลดความเสี่ยงจากการย้ายระบบครั้งเดียวทั้งหมด

## 3. ขอบเขตและบทบาทของ `original-web`

`original-web/` ใช้เก็บสำเนาเว็บไซต์ Pure PHP เดิมเพื่ออ้างอิงระหว่างการพัฒนาเท่านั้น เช่น:

- ขั้นตอนการทำงานเดิม
- เงื่อนไขทางธุรกิจ
- SQL และโครงสร้างข้อมูลเดิม
- รูปแบบหน้าจอและข้อความที่ผู้ใช้คุ้นเคย
- การเชื่อมต่อบริการภายนอก
- ไฟล์รูปภาพ CSS JavaScript และ Asset ที่ต้องพิจารณานำกลับมาใช้

ข้อกำหนดสำหรับโฟลเดอร์นี้:

- ห้ามใช้ไฟล์จาก `original-web/` เป็น Runtime ของระบบใหม่
- ห้ามแก้โค้ดเดิมเพื่อเพิ่มฟีเจอร์ใหม่ เว้นแต่จำเป็นต้องแก้ปัญหาระบบ Production ระหว่างช่วงเปลี่ยนผ่าน
- ห้ามอ้างอิงไฟล์เดิมด้วย `include` หรือ `require` จาก Laravel
- ห้ามนำรหัสผ่าน API key หรือไฟล์ตั้งค่าที่เป็นความลับเข้าระบบ Version Control
- ให้ใช้เป็นข้อมูลอ้างอิง และเขียนฟังก์ชันใหม่ตามมาตรฐาน Laravel

โครงสร้างที่แนะนำเมื่อเพิ่มเว็บไซต์เดิมเข้ามา:

```text
original-web/
├── README.md              # วิธีรันระบบเดิมและข้อมูลที่ต้องทราบ
├── public/                # Entry point และ public assets เดิม
├── includes/              # ไฟล์ include/helper เดิม
├── admin/                 # ส่วนผู้ดูแลระบบเดิม (ถ้ามี)
├── sql/                   # Schema หรือ SQL สำหรับอ้างอิง โดยต้องไม่มีข้อมูลลับ
└── docs/                  # ภาพหน้าจอและคำอธิบาย Flow เดิม
```

หากโครงสร้างจริงแตกต่างจากตัวอย่าง ให้รักษาโครงสร้างเดิมไว้เพื่อให้ตรวจสอบย้อนหลังได้ง่าย

## 4. สถาปัตยกรรมเป้าหมาย

ระบบใหม่ใช้แนวทาง API-first โดยมีภาพรวมดังนี้:

```text
Web UI / Admin UI / Mobile App
              │
              ▼
        REST API (/api/v1)
              │
              ▼
Controller → Form Request → Action/Service → Model/Repository
                                      │
                                      ▼
                            Database / External Service
```

หน้าที่ของแต่ละส่วน:

- **Routes**: กำหนด URL, API version และ Middleware
- **Controller**: รับ Request เรียก Use Case และส่ง Response โดยไม่ใส่ Business Logic จำนวนมาก
- **Form Request**: ตรวจสอบและอนุญาตข้อมูลที่รับเข้ามา
- **Action/Service**: จัดการ Business Logic และ Transaction
- **Model**: แทน Entity และความสัมพันธ์ของข้อมูลผ่าน Eloquent
- **API Resource**: กำหนดรูปแบบ JSON ที่ส่งให้ Client อย่างสม่ำเสมอ
- **Policy/Gate**: ควบคุมสิทธิ์การเข้าถึงข้อมูลและคำสั่ง
- **Job/Event/Listener**: รองรับงานเบื้องหลังและลดการผูกกันของแต่ละโมดูล
- **Repository**: ใช้เฉพาะกรณีที่ Query ซับซ้อน ต้องสลับ Data Source หรือช่วยแยกขอบเขตได้จริง ไม่จำเป็นต้องสร้างให้ทุก Model

## 5. โครงสร้าง Laravel ที่แนะนำ

```text
app/
├── Actions/               # Use case หรือคำสั่งทางธุรกิจ
├── Enums/                 # ค่าคงที่เชิงโดเมน
├── Events/
├── Exceptions/
├── Http/
│   ├── Controllers/
│   │   ├── Api/V1/
│   │   └── Web/
│   ├── Middleware/
│   ├── Requests/Api/V1/
│   └── Resources/Api/V1/
├── Jobs/
├── Listeners/
├── Models/
├── Policies/
└── Services/              # งานเชื่อมต่อระบบภายนอกหรือ Logic ที่ใช้ร่วมกัน

routes/
├── api.php                # API สำหรับ Web client และ Mobile app
├── web.php                # หน้าเว็บ, session และ route ที่ไม่ใช่ API
└── console.php

tests/
├── Feature/Api/V1/
└── Unit/
```

ไม่จำเป็นต้องสร้างทุกโฟลเดอร์ล่วงหน้า ให้เพิ่มเมื่อเริ่มมีโค้ดที่รับผิดชอบงานนั้นจริง

## 6. มาตรฐาน API

### 6.1 URL และ Version

- ใช้คำนามแบบพหูพจน์ เช่น `/api/v1/products`
- ระบุ Version ใน URL เช่น `/api/v1`
- ใช้ HTTP Method ตามความหมาย:
  - `GET` อ่านข้อมูล
  - `POST` สร้างข้อมูลหรือสั่งทำงาน
  - `PUT/PATCH` แก้ไขข้อมูล
  - `DELETE` ลบข้อมูล
- หลีกเลี่ยงการใส่คำกริยาใน URL ยกเว้นคำสั่งที่ไม่ใช่ CRUD โดยธรรมชาติ เช่น `/orders/{order}/cancel`

### 6.2 รูปแบบ Response

ตัวอย่างข้อมูลสำเร็จ:

```json
{
  "data": {
    "id": 1,
    "name": "Example"
  }
}
```

ตัวอย่างรายการพร้อม Pagination:

```json
{
  "data": [],
  "links": {},
  "meta": {}
}
```

ตัวอย่าง Validation Error:

```json
{
  "message": "The given data was invalid.",
  "errors": {
    "email": ["กรุณาระบุอีเมลให้ถูกต้อง"]
  }
}
```

ให้ใช้ HTTP Status Code ให้ถูกต้อง เช่น `200`, `201`, `204`, `400`, `401`, `403`, `404`, `422` และ `500`

### 6.3 Authentication และความปลอดภัย

- พิจารณาใช้ Laravel Sanctum สำหรับ Token ของ Mobile app และ First-party client
- เก็บรหัสผ่านด้วย Hash ของ Laravel เท่านั้น
- ใช้ Policy หรือ Gate ตรวจสิทธิ์ในทุก Resource ที่เป็นข้อมูลส่วนบุคคลหรือข้อมูลสำคัญ
- ใช้ Rate Limit กับ Login, OTP และ API ที่อาจถูกโจมตีหรือเรียกซ้ำจำนวนมาก
- Validate ข้อมูลทุก Request และไม่เชื่อถือข้อมูลจาก Client
- ไม่ส่ง Stack trace, SQL, Token หรือข้อมูลลับกลับไปใน Production
- เก็บ Secret ใน `.env` และตั้งค่าผ่าน `config/*`
- กำหนด CORS เฉพาะ Origin และ Method ที่จำเป็น

## 7. แนวทางย้ายระบบ

### ระยะที่ 1: สำรวจระบบเดิม

- นำ Source Code เดิมมาเก็บใน `original-web/`
- ทำรายการหน้าเว็บ ฟีเจอร์ URL และสิทธิ์ผู้ใช้ทั้งหมด
- ทำรายการฐานข้อมูล ตาราง ความสัมพันธ์ Trigger และ Stored Procedure
- ระบุ Cron job, Email, Payment, File upload และบริการภายนอก
- บันทึก Flow สำคัญพร้อม Input, Output และกรณีผิดพลาด
- แยกฟีเจอร์เป็น `ต้องย้าย`, `ปรับใหม่`, `รวม`, หรือ `ยกเลิก`

ผลลัพธ์ที่ควรได้คือ Migration Matrix ของแต่ละโมดูล:

| โมดูลเดิม | Entry point เดิม | ตารางที่ใช้ | API ใหม่ | สถานะ | หมายเหตุ |
|---|---|---|---|---|---|
| หน้า Home | `index.php` | `news` | รอกำหนด Product/News API | ขึ้นโครงแล้ว | แยก Blade layout, header และ footer แล้ว |
| ตัวอย่าง: ผู้ใช้งาน | `login.php` | `users` | `POST /api/v1/auth/login` | รอวิเคราะห์ | ตรวจรูปแบบรหัสผ่านเดิม |

### ระยะที่ 2: ออกแบบข้อมูลและ API

- จัดทำ ER Diagram จากฐานข้อมูลเดิม
- กำหนด Laravel Migration โดยไม่ผูกกับ SQL ในหน้า PHP เดิม
- วางแผน Mapping ข้อมูลเดิมไป Schema ใหม่
- กำหนด API Contract ก่อนพัฒนาหน้าเว็บหรือแอป
- กำหนด Role และ Permission
- กำหนดรูปแบบ Error, Pagination, Filter, Sort และ Search ให้เหมือนกันทุกโมดูล

### ระยะที่ 3: สร้างระบบพื้นฐาน

- เปิดใช้ `routes/api.php` ใน `bootstrap/app.php`
- ติดตั้งและตั้งค่า Authentication ที่เลือกใช้
- สร้างโครงสร้าง Error Response และ API Resource กลาง
- ตั้งค่า Database, Queue, Cache, Mail, Storage และ Logging
- เตรียม CI สำหรับ Lint, Test และ Build
- เตรียม Environment แยก Local, Staging และ Production

### ระยะที่ 4: ย้ายทีละโมดูล

สำหรับแต่ละโมดูลให้ทำตามลำดับ:

1. อ่าน Flow และ SQL จากระบบเดิม
2. เขียน Test เพื่อระบุพฤติกรรมที่ต้องรักษา
3. สร้างหรือปรับ Migration และ Model
4. เขียน Action/Service และ Policy
5. สร้าง API Controller, Form Request และ API Resource
6. เขียน Feature Test ของ API
7. เชื่อมต่อหน้าเว็บใหม่กับ API
8. เปรียบเทียบผลลัพธ์กับระบบเดิม
9. ให้ผู้เกี่ยวข้องทดสอบและอนุมัติก่อนเปิดใช้งาน

### ระยะที่ 5: ย้ายข้อมูลและเปิดใช้งาน

- สร้างคำสั่ง Import ที่รันซ้ำได้อย่างปลอดภัย
- เก็บ Log ของจำนวนข้อมูลที่อ่าน สำเร็จ ข้าม และผิดพลาด
- ทดสอบ Migration ด้วยสำเนาฐานข้อมูลจริงที่ปกปิดข้อมูลสำคัญแล้ว
- ตรวจยอดรวมและความสัมพันธ์ของข้อมูลก่อนและหลังย้าย
- กำหนดช่วงหยุดเขียนข้อมูลหรือวิธี Sync ระหว่างระบบเดิมกับระบบใหม่
- เตรียม Rollback plan และ Backup ที่ผ่านการทดสอบการกู้คืนแล้ว
- เปิดใช้งานแบบทีละโมดูลหรือกลุ่มผู้ใช้ หากระบบรองรับ

### ระยะที่ 6: ปิดระบบเดิม

- ยืนยันว่าไม่มี Traffic หรือ Background job ที่ยังเรียกระบบเดิม
- เก็บฐานข้อมูลและ Source Code เดิมตามนโยบายการเก็บรักษาข้อมูล
- ปิด Credential และ Integration ที่ไม่ใช้แล้ว
- เปลี่ยน `original-web/` เป็น Archive แบบอ่านอย่างเดียว
- อัปเดตเอกสาร Operation และคู่มือผู้ใช้

## 8. ฐานข้อมูลและการย้ายข้อมูล

### 8.1 การป้องกันผลกระทบต่อเว็บและฐานข้อมูลเดิม

> **ข้อกำหนดสำคัญ:** ห้ามรันคำสั่ง สคริปต์ Migration, Seeder, SQL, Queue, Cron หรือโค้ดใด ๆ ที่อาจเขียน แก้ไข หรือลบข้อมูลของเว็บเดิมและฐานข้อมูลเดิม

- ไฟล์เชื่อมต่อฐานข้อมูลภายใน `original-web/` เช่น `connect_db/Control_Connect*.php` ใช้สำหรับอ่านและศึกษาโครงสร้างเดิมเท่านั้น ห้ามนำมาเรียกใช้งานจากระบบ Laravel
- การพัฒนา การทดสอบ Migration, Seeder และคำสั่ง Artisan ที่เกี่ยวข้องกับฐานข้อมูล ต้องใช้ฐานข้อมูลใหม่ตามค่า `DB_*` ที่ตั้งไว้ในไฟล์ `.env` ของโปรเจกต์ Laravel นี้เท่านั้น
- ก่อนรันคำสั่งที่เปลี่ยนแปลงฐานข้อมูล ต้องตรวจสอบให้แน่ใจว่า Laravel เชื่อมต่อไปยังฐานข้อมูลใหม่จาก `.env` และไม่ใช่ Host, Database หรือ Credential ของระบบเดิม
- ห้ามคัดลอก Credential จากเว็บเดิมมาใส่ใน `.env` โดยไม่ได้รับอนุญาตอย่างชัดเจน
- หากไม่สามารถยืนยันปลายทางของฐานข้อมูลได้ ให้หยุดและรายงานก่อนรันคำสั่งเสมอ

- ห้ามแก้ฐานข้อมูล Production ด้วยมือโดยไม่มี Migration หรือ Runbook
- ใช้ Laravel Migration เป็นประวัติ Schema ของระบบใหม่
- ใช้ Foreign Key และ Index ตามรูปแบบ Query ที่ใช้งานจริง
- ใช้ Transaction กับคำสั่งที่เปลี่ยนข้อมูลหลายส่วนและต้องสำเร็จพร้อมกัน
- หลีกเลี่ยงการเปลี่ยน Primary Key เดิมจนกว่าจะวางแผน Mapping ครบถ้วน
- หากต้องสร้าง ID ใหม่ ให้เก็บ Legacy ID เพื่อ Trace และตรวจสอบข้อมูลย้อนหลัง
- วันที่และเวลาใน API ควรมี Timezone ชัดเจน และแนะนำให้เก็บในฐานข้อมูลเป็น UTC
- ไฟล์จากระบบเดิมควรถูกตรวจสอบชนิด ขนาด ชื่อไฟล์ และสิทธิ์ก่อนนำเข้า Storage ใหม่

## 9. แนวทางรองรับแอปมือถือ

- Mobile app ต้องเรียกใช้ API เดียวกับ Web client และไม่เชื่อมฐานข้อมูลโดยตรง
- API Response ต้องไม่ขึ้นกับ HTML หรือ Session ของหน้าเว็บ
- รองรับ Token แยกตามอุปกรณ์และสามารถ Revoke รายอุปกรณ์ได้
- ออกแบบ Endpoint ให้ลดจำนวน Request ที่ไม่จำเป็น แต่ไม่รวมข้อมูลหลายโดเมนจนดูแลยาก
- ใช้ Pagination กับรายการข้อมูลทั้งหมดที่มีโอกาสเติบโต
- รองรับการ Retry โดยไม่สร้างข้อมูลซ้ำสำหรับคำสั่งสำคัญ เช่น การชำระเงินหรือสร้างคำสั่งซื้อ ด้วย Idempotency key
- วางแผน Push notification, Deep link และ Device registration เป็นโมดูลแยก
- รักษา API Version เดิมในช่วงที่ผู้ใช้ยังไม่อัปเดตแอป และกำหนดนโยบายเลิกใช้งาน Version ให้ชัดเจน

## 10. การทดสอบและเกณฑ์คุณภาพ

- **Unit Test**: Business rule และ Logic ที่ไม่ต้องเรียก HTTP
- **Feature/API Test**: Authentication, Authorization, Validation, Response และ Database effect
- **Integration Test**: Payment, Email, Storage และบริการภายนอก โดยใช้ Sandbox หรือ Fake เมื่อเหมาะสม
- **Migration Test**: ตรวจจำนวนข้อมูล Mapping และกรณีข้อมูลเดิมไม่สมบูรณ์
- **Regression Test**: เปรียบเทียบ Flow สำคัญกับ Pure PHP เดิม

Definition of Done ของแต่ละ API:

- มี Validation และ Authorization
- มีรูปแบบ Response ตามมาตรฐาน
- มี Test สำหรับกรณีสำเร็จ กรณีข้อมูลผิด และกรณีไม่มีสิทธิ์
- ไม่มีข้อมูลลับใน Log หรือ Response
- มีเอกสาร Endpoint และตัวอย่าง Request/Response
- ผ่าน Code style, Test และการ Review

## 11. ลำดับความสำคัญที่แนะนำ

1. สำรวจและทำรายการระบบเดิมใน `original-web/`
2. Authentication, User, Role และ Permission
3. Master data ที่โมดูลอื่นต้องใช้
4. ฟีเจอร์หลักของธุรกิจตาม Dependency
5. งาน Upload, Notification, Queue และ Integration ภายนอก
6. Admin และ Report
7. Data migration, UAT, Cutover และ Archive ระบบเดิม

## 12. Checklist ก่อนเริ่มพัฒนาแต่ละโมดูล

- [ ] ระบุหน้าและไฟล์ที่เกี่ยวข้องใน `original-web/`
- [ ] เข้าใจ Business rule และสิทธิ์ของผู้ใช้
- [ ] ระบุตารางและข้อมูลที่เกี่ยวข้อง
- [ ] ตัดสินใจว่าจะรักษา ปรับ หรือยกเลิกพฤติกรรมเดิม
- [ ] ออกแบบ Endpoint และ Response
- [ ] ออกแบบ Migration และแผนย้ายข้อมูล
- [ ] ระบุผลกระทบต่อ Web และ Mobile
- [ ] เขียน Test cases
- [ ] ระบุวิธี Rollback
- [ ] อัปเดต Migration Matrix และเอกสาร API

## 13. งานถัดไป

เมื่อเพิ่มเว็บไซต์เก่าลงใน `original-web/` แล้ว ควรเริ่มจาก:

1. สรุป Tree ของไฟล์และ Entry point ทั้งหมด
2. ค้นหา Database connection, SQL, Session, Cookie และจุดเชื่อมต่อภายนอก
3. สร้างรายการโมดูลและ Migration Matrix จากโค้ดจริง
4. เลือกโมดูลแรกที่ Dependency ต่ำแต่มีคุณค่าต่อผู้ใช้
5. สร้าง API Contract และ Test ของโมดูลนั้นก่อนเริ่มเขียนหน้าเว็บใหม่

## 14. ข้อกำหนดด้านการแสดงผลเหมือนระบบเดิม (Visual Parity)

หน้าเว็บใหม่ต้องแสดงผลและใช้งานได้เหมือนเว็บไซต์เดิมก่อน จึงค่อยพิจารณาปรับ Design หรือ User Experience ในภายหลัง การย้ายโค้ดเข้าสู่ Laravel ไม่ใช่การอนุญาตให้ออกแบบหน้าใหม่โดยอัตโนมัติ

ข้อกำหนดสำหรับแต่ละหน้าที่นำมาจาก `original-web/`:

- ต้องรักษาโครงสร้าง Header, Navigation, Side navigation, Content และ Footer ให้เหมือนเดิม
- ต้องรักษาข้อความ ลำดับสินค้า รูปภาพ สี ฟอนต์ ระยะห่าง ขนาด และตำแหน่งขององค์ประกอบให้ใกล้เคียงระบบเดิมมากที่สุด
- ต้องรักษา Responsive behavior ของ Desktop, Tablet และ Mobile
- ต้องรักษา Interaction เดิม เช่น Mobile menu, Slider, Lazy loading, Popup และ Review slider
- ต้องรักษา URL, SEO title, Meta description, Structured data และลิงก์ที่ระบบเดิมใช้งาน
- Business rule, ข้อมูล Dynamic, ภาษา และ Permission ต้องให้ผลลัพธ์เหมือนเดิม แม้ Implementation ภายในจะเปลี่ยนเป็น Laravel และ API
- ห้ามนำไฟล์ PHP เดิมมา `include` หรือรันเป็นส่วนหนึ่งของ Laravel ให้แปลงเป็น Blade, Controller, Service หรือ API ตามหน้าที่
- ต้องเปรียบเทียบ Screenshot ระหว่างเว็บเดิมกับเว็บใหม่ที่ Viewport เดียวกันอย่างน้อย Desktop และ Mobile ก่อนอนุมัติแต่ละหน้า
- หากไฟล์หรือข้อมูลจากระบบเดิมไม่ครบ ต้องรวบรวมรายชื่อและแจ้งผู้ดูแลระหว่างทำงานก่อนสร้างสิ่งทดแทนหรือเดาพฤติกรรมเอง

> **แนวทางการรายงาน:** หากพบว่าไฟล์หรือข้อมูลใน `original-web/` ไม่ครบระหว่างย้ายแต่ละหน้า ให้หยุดเฉพาะส่วนที่ได้รับผลกระทบและรายงานรายชื่อไฟล์ที่ขาดให้ผู้ดูแลทราบในระหว่างทำงาน โดยไม่บันทึกรายการ Missing dependency แบบชั่วคราวลงในเอกสารนี้

เมื่อได้รับไฟล์ที่ขาดแล้ว ให้ตรวจสอบหน้าเดิมอีกครั้งก่อนดำเนินการต่อ และห้ามเดาเนื้อหา รูปแบบ หรือพฤติกรรมของไฟล์ที่ไม่มีต้นฉบับ

---

เอกสารนี้เป็นแผนตั้งต้นและต้องปรับปรุงต่อเมื่อมี Source Code เดิม โครงสร้างฐานข้อมูล และข้อกำหนดทางธุรกิจที่ชัดเจนขึ้น
